/******************************************************************************
 *
 *             Copyright 2016 Mentor Graphics Corporation
 *                        All rights reserved
 *
 * THIS WORK CONTAINS TRADE SECRET AND PROPRIETARY INFORMATION WHICH IS THE
 * PROPERTY OF MENTOR GRAPHICS CORPORATION OR ITS LICENSORS AND IS SUBJECT TO
 * LICENSE TERMS.
 *
 ******************************************************************************
 * This file is automatically generated by Configuration tool.
 * Do not edit manually.
 * Generated by VSC tool Version 2.5.4
 *
 *=============================================================================
 *
 * Module:       CanIf
 *
 * File Name:    CanIf_PbCfg.c
 *
 * Description:  Lists the Post build time configurable parameters.
 *
 *****************************************************************************/

/*=============================== INCLUSIONS ================================*/
#include "CanIf_Private.h"
#include "CanIf.h"
#include "CanIf_PBcfg.h"

/*============================== VERSION CHECK ==============================*/


/*=========================== EXTERNAL CONSTANTS ============================*/


#define CANIF_START_SEC_POSTBUILD_VAR_NO_INIT_UNSPECIFIED  /* PRQA S 3614 */ 
#include "CanIf_MemMap.h"  /* PRQA S 5087 */ /* KW MISRA.INCL.INSIDE */







#define CANIF_STOP_SEC_POSTBUILD_VAR_NO_INIT_UNSPECIFIED  /* PRQA S 3614 */ 
#include "CanIf_MemMap.h"  /* PRQA S 5087 */ /* KW MISRA.INCL.INSIDE */


#define CANIF_START_SEC_POSTBUILD_CONST_UNSPECIFIED  /* PRQA S 3614 */ 
#include "CanIf_MemMap.h"  /* PRQA S 5087 */ /* KW MISRA.INCL.INSIDE */

/**
  * \brief This container contains the init parameters of the CAN Interface
  *  It contains Postbuild time configurable parameters
  *
  */
static CONST(CanIfInitConfiguration_t, CANIF_CONST_PB) 
CanIf_InitConfiguration0 =
{
    /**< Total number of CanRxPduIds to be handled */
    59,
    /**< Total number of CanTxPduIds to be handled */
    9,
    /**< Total number of dynamic CanTxPduIds to be handled */
    0,
    /**< Total number of CAN Drivers to be handled */
    1
};




/**
  * \brief This container contains the Postbuild time configurable
  *  buffer configuration
  *
  */
static CONST(CanIfBufferConfig_t, CANIF_CONST_PB) 
CanIfBufferCfg0[9] =
{
    /* Buffer 0 */
    {
    /**< Handle to buffer Data */
         NULL_PTR,
    /**< Handle to hthCfg */
        0,
    /**< number of CanIf Tx L-PDUs which can be buffered in one Txbuffer */
        0,
    /**< Pointer to data pointer list */
         NULL_PTR,
    /**< MaxDataSize */
        8,
    /**< IsDedicated */
        STD_OFF
    }, 
    /* Buffer 1 */
    {
    /**< Handle to buffer Data */
         NULL_PTR,
    /**< Handle to hthCfg */
        1,
    /**< number of CanIf Tx L-PDUs which can be buffered in one Txbuffer */
        0,
    /**< Pointer to data pointer list */
         NULL_PTR,
    /**< MaxDataSize */
        8,
    /**< IsDedicated */
        STD_OFF
    }, 
    /* Buffer 2 */
    {
    /**< Handle to buffer Data */
         NULL_PTR,
    /**< Handle to hthCfg */
        2,
    /**< number of CanIf Tx L-PDUs which can be buffered in one Txbuffer */
        0,
    /**< Pointer to data pointer list */
         NULL_PTR,
    /**< MaxDataSize */
        8,
    /**< IsDedicated */
        STD_OFF
    }, 
    /* Buffer 3 */
    {
    /**< Handle to buffer Data */
         NULL_PTR,
    /**< Handle to hthCfg */
        3,
    /**< number of CanIf Tx L-PDUs which can be buffered in one Txbuffer */
        0,
    /**< Pointer to data pointer list */
         NULL_PTR,
    /**< MaxDataSize */
        8,
    /**< IsDedicated */
        STD_OFF
    }, 
    /* Buffer 4 */
    {
    /**< Handle to buffer Data */
         NULL_PTR,
    /**< Handle to hthCfg */
        4,
    /**< number of CanIf Tx L-PDUs which can be buffered in one Txbuffer */
        0,
    /**< Pointer to data pointer list */
         NULL_PTR,
    /**< MaxDataSize */
        8,
    /**< IsDedicated */
        STD_OFF
    }, 
    /* Buffer 5 */
    {
    /**< Handle to buffer Data */
         NULL_PTR,
    /**< Handle to hthCfg */
        5,
    /**< number of CanIf Tx L-PDUs which can be buffered in one Txbuffer */
        0,
    /**< Pointer to data pointer list */
         NULL_PTR,
    /**< MaxDataSize */
        8,
    /**< IsDedicated */
        STD_OFF
    }, 
    /* Buffer 6 */
    {
    /**< Handle to buffer Data */
         NULL_PTR,
    /**< Handle to hthCfg */
        6,
    /**< number of CanIf Tx L-PDUs which can be buffered in one Txbuffer */
        0,
    /**< Pointer to data pointer list */
         NULL_PTR,
    /**< MaxDataSize */
        8,
    /**< IsDedicated */
        STD_OFF
    }, 
    /* Buffer 7 */
    {
    /**< Handle to buffer Data */
         NULL_PTR,
    /**< Handle to hthCfg */
        7,
    /**< number of CanIf Tx L-PDUs which can be buffered in one Txbuffer */
        0,
    /**< Pointer to data pointer list */
         NULL_PTR,
    /**< MaxDataSize */
        8,
    /**< IsDedicated */
        STD_OFF
    }, 
    /* Buffer 8 */
    {
    /**< Handle to buffer Data */
         NULL_PTR,
    /**< Handle to hthCfg */
        8,
    /**< number of CanIf Tx L-PDUs which can be buffered in one Txbuffer */
        0,
    /**< Pointer to data pointer list */
         NULL_PTR,
    /**< MaxDataSize */
        8,
    /**< IsDedicated */
        STD_OFF
    }

};

/**
  * \brief This container contains the Postbuild time configurable
  *  parameters of each transmit CAN L-PDU
  *
  */
static CONST(CanIfTxPduConfigInfo_t, CANIF_CONST_PB) 
CanIf_TxPduConfigInfo0_a[9] =
{
    /* TxPDU 0 */
    {
    /**< CAN Identifier of transmit CAN L-PDUs used by the CAN Driver */
        0x80ff7101U,
    /**< Defines the type of each transmit CAN L-PDU STATIC/DYNAMIC */
        CANIF_TXPDU_STATIC,
    /**< CAN Identifier type of transmit CAN L-PDUs used by the CAN Driver for
       CAN L-PDU transmission EXTENDED/STANDARD */
        CANIF_TX_CANID_EXTENDED,
    /**< Specifies the type of transmit buffering configured for the
       corresponding TxPdu */
        CANIF_NO_BUFFER,
    /**< Handle, that defines the TxBuffer Related to this TxPdu */
        &CanIfBufferCfg0[0],
    /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        5,
#if(CANIF_SETDYNAMICTXID_API == STD_ON)
    /**< Index to the array, where dynamic Can-ID is stored */
        0,
#endif
    /**< Data length code (in bytes) of transmit CAN L-PDUs used by the
         CAN Driver */
        8,
    /**< Defines the index to Tx PDU Callback */
        1, 
    /**< Enables and disables transmit confirmation for each transmit CAN L-PDU
       for reading its notification status */
        STD_OFF,
    /**< Pointer to the pointer to TxBuffer data related to this TxPdu */
        NULL_PTR,
    /**< Tx PDU's dedicated Tx buffer data size */
        0,
    /**< Defines whether the TxBuffer related to this TxPdu is dedicated or not */
        STD_OFF,
#if(CANIF_PN_SUPPORT == STD_ON)
    /**< Enables and disables PnFilterPdu */
        STD_OFF,
#endif
#if(CANIF_CHECKSUM_TX_CALLOUT_SUPPORT == STD_ON)
    /**< Enables and disables checksum calculation and for each transmit CAN L-PDU */
        STD_OFF
#endif
    }, 
    /* TxPDU 1 */
    {
    /**< CAN Identifier of transmit CAN L-PDUs used by the CAN Driver */
        0x80ff6001U,
    /**< Defines the type of each transmit CAN L-PDU STATIC/DYNAMIC */
        CANIF_TXPDU_STATIC,
    /**< CAN Identifier type of transmit CAN L-PDUs used by the CAN Driver for
       CAN L-PDU transmission EXTENDED/STANDARD */
        CANIF_TX_CANID_EXTENDED,
    /**< Specifies the type of transmit buffering configured for the
       corresponding TxPdu */
        CANIF_NO_BUFFER,
    /**< Handle, that defines the TxBuffer Related to this TxPdu */
        &CanIfBufferCfg0[1],
    /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        4,
#if(CANIF_SETDYNAMICTXID_API == STD_ON)
    /**< Index to the array, where dynamic Can-ID is stored */
        0,
#endif
    /**< Data length code (in bytes) of transmit CAN L-PDUs used by the
         CAN Driver */
        8,
    /**< Defines the index to Tx PDU Callback */
        1, 
    /**< Enables and disables transmit confirmation for each transmit CAN L-PDU
       for reading its notification status */
        STD_OFF,
    /**< Pointer to the pointer to TxBuffer data related to this TxPdu */
        NULL_PTR,
    /**< Tx PDU's dedicated Tx buffer data size */
        0,
    /**< Defines whether the TxBuffer related to this TxPdu is dedicated or not */
        STD_OFF,
#if(CANIF_PN_SUPPORT == STD_ON)
    /**< Enables and disables PnFilterPdu */
        STD_OFF,
#endif
#if(CANIF_CHECKSUM_TX_CALLOUT_SUPPORT == STD_ON)
    /**< Enables and disables checksum calculation and for each transmit CAN L-PDU */
        STD_OFF
#endif
    }, 
    /* TxPDU 2 */
    {
    /**< CAN Identifier of transmit CAN L-PDUs used by the CAN Driver */
        0x80ff6002U,
    /**< Defines the type of each transmit CAN L-PDU STATIC/DYNAMIC */
        CANIF_TXPDU_STATIC,
    /**< CAN Identifier type of transmit CAN L-PDUs used by the CAN Driver for
       CAN L-PDU transmission EXTENDED/STANDARD */
        CANIF_TX_CANID_EXTENDED,
    /**< Specifies the type of transmit buffering configured for the
       corresponding TxPdu */
        CANIF_NO_BUFFER,
    /**< Handle, that defines the TxBuffer Related to this TxPdu */
        &CanIfBufferCfg0[2],
    /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        1,
#if(CANIF_SETDYNAMICTXID_API == STD_ON)
    /**< Index to the array, where dynamic Can-ID is stored */
        0,
#endif
    /**< Data length code (in bytes) of transmit CAN L-PDUs used by the
         CAN Driver */
        8,
    /**< Defines the index to Tx PDU Callback */
        1, 
    /**< Enables and disables transmit confirmation for each transmit CAN L-PDU
       for reading its notification status */
        STD_OFF,
    /**< Pointer to the pointer to TxBuffer data related to this TxPdu */
        NULL_PTR,
    /**< Tx PDU's dedicated Tx buffer data size */
        0,
    /**< Defines whether the TxBuffer related to this TxPdu is dedicated or not */
        STD_OFF,
#if(CANIF_PN_SUPPORT == STD_ON)
    /**< Enables and disables PnFilterPdu */
        STD_OFF,
#endif
#if(CANIF_CHECKSUM_TX_CALLOUT_SUPPORT == STD_ON)
    /**< Enables and disables checksum calculation and for each transmit CAN L-PDU */
        STD_OFF
#endif
    }, 
    /* TxPDU 3 */
    {
    /**< CAN Identifier of transmit CAN L-PDUs used by the CAN Driver */
        0x80ff3101U,
    /**< Defines the type of each transmit CAN L-PDU STATIC/DYNAMIC */
        CANIF_TXPDU_STATIC,
    /**< CAN Identifier type of transmit CAN L-PDUs used by the CAN Driver for
       CAN L-PDU transmission EXTENDED/STANDARD */
        CANIF_TX_CANID_EXTENDED,
    /**< Specifies the type of transmit buffering configured for the
       corresponding TxPdu */
        CANIF_NO_BUFFER,
    /**< Handle, that defines the TxBuffer Related to this TxPdu */
        &CanIfBufferCfg0[3],
    /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        6,
#if(CANIF_SETDYNAMICTXID_API == STD_ON)
    /**< Index to the array, where dynamic Can-ID is stored */
        0,
#endif
    /**< Data length code (in bytes) of transmit CAN L-PDUs used by the
         CAN Driver */
        8,
    /**< Defines the index to Tx PDU Callback */
        1, 
    /**< Enables and disables transmit confirmation for each transmit CAN L-PDU
       for reading its notification status */
        STD_OFF,
    /**< Pointer to the pointer to TxBuffer data related to this TxPdu */
        NULL_PTR,
    /**< Tx PDU's dedicated Tx buffer data size */
        0,
    /**< Defines whether the TxBuffer related to this TxPdu is dedicated or not */
        STD_OFF,
#if(CANIF_PN_SUPPORT == STD_ON)
    /**< Enables and disables PnFilterPdu */
        STD_OFF,
#endif
#if(CANIF_CHECKSUM_TX_CALLOUT_SUPPORT == STD_ON)
    /**< Enables and disables checksum calculation and for each transmit CAN L-PDU */
        STD_OFF
#endif
    }, 
    /* TxPDU 4 */
    {
    /**< CAN Identifier of transmit CAN L-PDUs used by the CAN Driver */
        0x80ff1101U,
    /**< Defines the type of each transmit CAN L-PDU STATIC/DYNAMIC */
        CANIF_TXPDU_STATIC,
    /**< CAN Identifier type of transmit CAN L-PDUs used by the CAN Driver for
       CAN L-PDU transmission EXTENDED/STANDARD */
        CANIF_TX_CANID_EXTENDED,
    /**< Specifies the type of transmit buffering configured for the
       corresponding TxPdu */
        CANIF_NO_BUFFER,
    /**< Handle, that defines the TxBuffer Related to this TxPdu */
        &CanIfBufferCfg0[4],
    /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        2,
#if(CANIF_SETDYNAMICTXID_API == STD_ON)
    /**< Index to the array, where dynamic Can-ID is stored */
        0,
#endif
    /**< Data length code (in bytes) of transmit CAN L-PDUs used by the
         CAN Driver */
        8,
    /**< Defines the index to Tx PDU Callback */
        1, 
    /**< Enables and disables transmit confirmation for each transmit CAN L-PDU
       for reading its notification status */
        STD_OFF,
    /**< Pointer to the pointer to TxBuffer data related to this TxPdu */
        NULL_PTR,
    /**< Tx PDU's dedicated Tx buffer data size */
        0,
    /**< Defines whether the TxBuffer related to this TxPdu is dedicated or not */
        STD_OFF,
#if(CANIF_PN_SUPPORT == STD_ON)
    /**< Enables and disables PnFilterPdu */
        STD_OFF,
#endif
#if(CANIF_CHECKSUM_TX_CALLOUT_SUPPORT == STD_ON)
    /**< Enables and disables checksum calculation and for each transmit CAN L-PDU */
        STD_OFF
#endif
    }, 
    /* TxPDU 5 */
    {
    /**< CAN Identifier of transmit CAN L-PDUs used by the CAN Driver */
        0x80ff1102U,
    /**< Defines the type of each transmit CAN L-PDU STATIC/DYNAMIC */
        CANIF_TXPDU_STATIC,
    /**< CAN Identifier type of transmit CAN L-PDUs used by the CAN Driver for
       CAN L-PDU transmission EXTENDED/STANDARD */
        CANIF_TX_CANID_EXTENDED,
    /**< Specifies the type of transmit buffering configured for the
       corresponding TxPdu */
        CANIF_NO_BUFFER,
    /**< Handle, that defines the TxBuffer Related to this TxPdu */
        &CanIfBufferCfg0[5],
    /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        3,
#if(CANIF_SETDYNAMICTXID_API == STD_ON)
    /**< Index to the array, where dynamic Can-ID is stored */
        0,
#endif
    /**< Data length code (in bytes) of transmit CAN L-PDUs used by the
         CAN Driver */
        8,
    /**< Defines the index to Tx PDU Callback */
        1, 
    /**< Enables and disables transmit confirmation for each transmit CAN L-PDU
       for reading its notification status */
        STD_OFF,
    /**< Pointer to the pointer to TxBuffer data related to this TxPdu */
        NULL_PTR,
    /**< Tx PDU's dedicated Tx buffer data size */
        0,
    /**< Defines whether the TxBuffer related to this TxPdu is dedicated or not */
        STD_OFF,
#if(CANIF_PN_SUPPORT == STD_ON)
    /**< Enables and disables PnFilterPdu */
        STD_OFF,
#endif
#if(CANIF_CHECKSUM_TX_CALLOUT_SUPPORT == STD_ON)
    /**< Enables and disables checksum calculation and for each transmit CAN L-PDU */
        STD_OFF
#endif
    }, 
    /* TxPDU 6 */
    {
    /**< CAN Identifier of transmit CAN L-PDUs used by the CAN Driver */
        0x80ff2101U,
    /**< Defines the type of each transmit CAN L-PDU STATIC/DYNAMIC */
        CANIF_TXPDU_STATIC,
    /**< CAN Identifier type of transmit CAN L-PDUs used by the CAN Driver for
       CAN L-PDU transmission EXTENDED/STANDARD */
        CANIF_TX_CANID_EXTENDED,
    /**< Specifies the type of transmit buffering configured for the
       corresponding TxPdu */
        CANIF_NO_BUFFER,
    /**< Handle, that defines the TxBuffer Related to this TxPdu */
        &CanIfBufferCfg0[6],
    /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        7,
#if(CANIF_SETDYNAMICTXID_API == STD_ON)
    /**< Index to the array, where dynamic Can-ID is stored */
        0,
#endif
    /**< Data length code (in bytes) of transmit CAN L-PDUs used by the
         CAN Driver */
        8,
    /**< Defines the index to Tx PDU Callback */
        1, 
    /**< Enables and disables transmit confirmation for each transmit CAN L-PDU
       for reading its notification status */
        STD_OFF,
    /**< Pointer to the pointer to TxBuffer data related to this TxPdu */
        NULL_PTR,
    /**< Tx PDU's dedicated Tx buffer data size */
        0,
    /**< Defines whether the TxBuffer related to this TxPdu is dedicated or not */
        STD_OFF,
#if(CANIF_PN_SUPPORT == STD_ON)
    /**< Enables and disables PnFilterPdu */
        STD_OFF,
#endif
#if(CANIF_CHECKSUM_TX_CALLOUT_SUPPORT == STD_ON)
    /**< Enables and disables checksum calculation and for each transmit CAN L-PDU */
        STD_OFF
#endif
    }, 
    /* TxPDU 7 */
    {
    /**< CAN Identifier of transmit CAN L-PDUs used by the CAN Driver */
        0x8cffa63dU,
    /**< Defines the type of each transmit CAN L-PDU STATIC/DYNAMIC */
        CANIF_TXPDU_STATIC,
    /**< CAN Identifier type of transmit CAN L-PDUs used by the CAN Driver for
       CAN L-PDU transmission EXTENDED/STANDARD */
        CANIF_TX_CANID_EXTENDED,
    /**< Specifies the type of transmit buffering configured for the
       corresponding TxPdu */
        CANIF_NO_BUFFER,
    /**< Handle, that defines the TxBuffer Related to this TxPdu */
        &CanIfBufferCfg0[7],
    /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        0,
#if(CANIF_SETDYNAMICTXID_API == STD_ON)
    /**< Index to the array, where dynamic Can-ID is stored */
        0,
#endif
    /**< Data length code (in bytes) of transmit CAN L-PDUs used by the
         CAN Driver */
        8,
    /**< Defines the index to Tx PDU Callback */
        1, 
    /**< Enables and disables transmit confirmation for each transmit CAN L-PDU
       for reading its notification status */
        STD_OFF,
    /**< Pointer to the pointer to TxBuffer data related to this TxPdu */
        NULL_PTR,
    /**< Tx PDU's dedicated Tx buffer data size */
        0,
    /**< Defines whether the TxBuffer related to this TxPdu is dedicated or not */
        STD_OFF,
#if(CANIF_PN_SUPPORT == STD_ON)
    /**< Enables and disables PnFilterPdu */
        STD_OFF,
#endif
#if(CANIF_CHECKSUM_TX_CALLOUT_SUPPORT == STD_ON)
    /**< Enables and disables checksum calculation and for each transmit CAN L-PDU */
        STD_OFF
#endif
    }, 
    /* TxPDU 8 */
    {
    /**< CAN Identifier of transmit CAN L-PDUs used by the CAN Driver */
        0x708U,
    /**< Defines the type of each transmit CAN L-PDU STATIC/DYNAMIC */
        CANIF_TXPDU_STATIC,
    /**< CAN Identifier type of transmit CAN L-PDUs used by the CAN Driver for
       CAN L-PDU transmission EXTENDED/STANDARD */
        CANIF_TX_CANID_STANDARD,
    /**< Specifies the type of transmit buffering configured for the
       corresponding TxPdu */
        CANIF_NO_BUFFER,
    /**< Handle, that defines the TxBuffer Related to this TxPdu */
        &CanIfBufferCfg0[8],
    /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        0,
#if(CANIF_SETDYNAMICTXID_API == STD_ON)
    /**< Index to the array, where dynamic Can-ID is stored */
        0,
#endif
    /**< Data length code (in bytes) of transmit CAN L-PDUs used by the
         CAN Driver */
        8,
    /**< Defines the index to Tx PDU Callback */
        0, 
    /**< Enables and disables transmit confirmation for each transmit CAN L-PDU
       for reading its notification status */
        STD_OFF,
    /**< Pointer to the pointer to TxBuffer data related to this TxPdu */
        NULL_PTR,
    /**< Tx PDU's dedicated Tx buffer data size */
        0,
    /**< Defines whether the TxBuffer related to this TxPdu is dedicated or not */
        STD_OFF,
#if(CANIF_PN_SUPPORT == STD_ON)
    /**< Enables and disables PnFilterPdu */
        STD_OFF,
#endif
#if(CANIF_CHECKSUM_TX_CALLOUT_SUPPORT == STD_ON)
    /**< Enables and disables checksum calculation and for each transmit CAN L-PDU */
        STD_OFF
#endif
    }
};




/**
  * \brief This container contains the Postbuild time configurable
  *  parameters of each receive CAN L-PDU
  *
  */
static CONST(CanIfRxPduConfigInfo_t, CANIF_CONST_PB) 
CanIf_RxPduConfigInfo0_a[59] =
{
    /* RxPDU 0 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        16724225,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        0,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        37,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        0, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 1 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419393808,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        9,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        46,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        1, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 2 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419392016,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        18,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        27,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        2, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 3 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419392160,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        27,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        3,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        3, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 4 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419392176,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        36,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        5,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        4, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 5 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419392192,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        45,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        0,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        5, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 6 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419392272,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        54,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        10,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        6, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 7 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419392416,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        63,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        30,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        7, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 8 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419392432,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        72,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        39,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        8, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 9 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419392448,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        81,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        40,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        9, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 10 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419392528,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        90,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        2,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        10, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 11 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419392672,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        99,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        24,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        11, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 12 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419392688,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        108,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        25,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        12, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 13 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419392704,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        117,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        19,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        13, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 14 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419389712,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        126,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        9,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        14, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 15 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419389856,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        135,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        49,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        15, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 16 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419389872,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        144,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        48,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        16, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 17 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419389888,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        153,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        50,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        17, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 18 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419389968,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        162,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        51,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        18, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 19 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419390112,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        171,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        54,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        19, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 20 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419390128,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        180,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        53,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        20, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 21 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419390144,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        189,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        52,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        21, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 22 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419390224,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        198,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        16,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        22, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 23 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419390368,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        207,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        33,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        23, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 24 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419390384,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        216,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        32,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        24, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 25 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419390400,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        225,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        36,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        25, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 26 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419390480,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        234,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        47,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        55, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 27 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419390624,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        243,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        1,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        55, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 28 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419390640,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        252,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        15,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        33, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 29 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419390656,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        261,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        14,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        26, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 30 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419390736,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        270,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        31,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        27, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 31 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419390880,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        279,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        34,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        30, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 32 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419390896,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        288,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        35,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        29, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 33 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419390912,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        297,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        38,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        32, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 34 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419390992,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        306,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        20,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        28, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 35 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419391136,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        315,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        29,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        54, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 36 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419391152,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        324,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        26,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        31, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 37 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419391168,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        333,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        28,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        36, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 38 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419391248,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        342,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        13,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        34, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 39 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419391392,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        351,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        21,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        35, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 40 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419391408,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        360,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        22,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        48, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 41 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419391424,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        369,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        23,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        38, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 42 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419391504,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        378,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        8,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        39, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 43 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419391648,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        387,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        45,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        37, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 44 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419391664,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        396,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        43,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        40, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 45 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419391680,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        405,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        44,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        43, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 46 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419391760,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        414,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        17,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        41, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 47 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419391904,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        423,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        4,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        42, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 48 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419391920,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        432,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        6,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        44, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 49 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419391936,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        441,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        7,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        46, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 50 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419377424,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        450,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        11,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        51, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 51 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419377440,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        459,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        12,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        49, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 52 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419369232,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        468,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        41,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        45, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 53 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419369248,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        477,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        42,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        47, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 54 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419373328,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        486,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        55,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        50, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 55 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        419373344,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        495,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        56,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        52, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 56 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        218081204,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_EXTENDED_NO_FD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        504,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        18,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        53, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        1,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_ON

    } ,     /* RxPDU 57 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        2015,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_STANDARD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        0,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        0,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        56, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        0,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_OFF

    } ,     /* RxPDU 58 */
    {
        /**< CAN Identifier of Receive CAN L-PDU */
        1792,
        /**< CAN Identifier type of receive CAN L-PDUs - EXTENDED/STANDARD */
        CANIF_RX_CANID_STANDARD,
/* if ReadRxPduData API is enabled */
#if(CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which CanIfRxPduReadData is enabled */
        0,

#endif /* End #if(CANIF_READRXPDU_DATA_API == STD_ON) */
/* if RxInterruptDecoupling is enabled */
#if(CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Holds the index of buffer for holding the received data values
         for which RxPduIntDecoupling is enabled */
        0,
#endif /* End #if(CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if Rx PDU CAN ID range support is enabled */
#if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON)

        /**< Reference to the CAN ID range information */
         NULL_PTR,
#endif /* End #if(CANIF_RX_PDU_RANGE_CFG_SUPPORT == STD_ON) */
        /**< Reference to the "global" Pdu structure to allow harmonization of
         handle IDs in the COM-Stack */
        1,
        /**< The HRH to which Rx L-PDU belongs to, is referred through this
         parameter */
        57, 
        /**< Data Length code of received CAN L-PDU */
        8,
        /**< Defines the index to Rx PDU Callback */
        0,
        /**< Enables and disables the Rx buffering for reading of received
         L-PDU data */
        STD_OFF,
        /**< Enables and disables receive indication for each receive
         CAN L-PDU for reading its notification status */
        STD_OFF,
        /**< Provides the reference to the CAN driver of corresponding TxPDU */
        0,
/* if Rx Checksum Callout support is enabled */
#if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON)
        /**< Enables and disables checksum calculation and checking for each receive CAN L-PDU */
        STD_OFF,
#endif /* End #if(CANIF_CHECKSUM_RX_CALLOUT_SUPPORT == STD_ON) */
        /**< Selects support for receive interrupt decoupling for
         corresponding RxPDU */
        STD_OFF

    } };



/**
  * \brief This buffer contains list of RxPdus for Hrh0 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh0[1] =
{
    /* RxPdu 0 */
    0    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh1 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh1[1] =
{
    /* RxPdu 0 */
    1    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh2 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh2[1] =
{
    /* RxPdu 0 */
    2    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh3 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh3[1] =
{
    /* RxPdu 0 */
    3    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh4 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh4[1] =
{
    /* RxPdu 0 */
    4    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh5 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh5[1] =
{
    /* RxPdu 0 */
    5    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh6 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh6[1] =
{
    /* RxPdu 0 */
    6    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh7 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh7[1] =
{
    /* RxPdu 0 */
    7    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh8 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh8[1] =
{
    /* RxPdu 0 */
    8    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh9 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh9[1] =
{
    /* RxPdu 0 */
    9    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh10 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh10[1] =
{
    /* RxPdu 0 */
    10    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh11 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh11[1] =
{
    /* RxPdu 0 */
    11    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh12 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh12[1] =
{
    /* RxPdu 0 */
    12    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh13 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh13[1] =
{
    /* RxPdu 0 */
    13    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh14 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh14[1] =
{
    /* RxPdu 0 */
    14    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh15 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh15[1] =
{
    /* RxPdu 0 */
    15    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh16 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh16[1] =
{
    /* RxPdu 0 */
    16    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh17 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh17[1] =
{
    /* RxPdu 0 */
    17    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh18 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh18[1] =
{
    /* RxPdu 0 */
    18    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh19 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh19[1] =
{
    /* RxPdu 0 */
    19    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh20 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh20[1] =
{
    /* RxPdu 0 */
    20    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh21 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh21[1] =
{
    /* RxPdu 0 */
    21    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh22 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh22[1] =
{
    /* RxPdu 0 */
    22    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh23 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh23[1] =
{
    /* RxPdu 0 */
    23    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh24 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh24[1] =
{
    /* RxPdu 0 */
    24    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh25 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh25[1] =
{
    /* RxPdu 0 */
    25    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh26 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh26[1] =
{
    /* RxPdu 0 */
    29    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh27 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh27[1] =
{
    /* RxPdu 0 */
    30    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh28 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh28[1] =
{
    /* RxPdu 0 */
    34    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh29 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh29[1] =
{
    /* RxPdu 0 */
    32    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh30 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh30[1] =
{
    /* RxPdu 0 */
    31    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh31 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh31[1] =
{
    /* RxPdu 0 */
    36    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh32 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh32[1] =
{
    /* RxPdu 0 */
    33    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh33 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh33[1] =
{
    /* RxPdu 0 */
    28    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh34 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh34[1] =
{
    /* RxPdu 0 */
    38    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh35 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh35[1] =
{
    /* RxPdu 0 */
    39    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh36 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh36[1] =
{
    /* RxPdu 0 */
    37    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh37 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh37[1] =
{
    /* RxPdu 0 */
    43    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh38 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh38[1] =
{
    /* RxPdu 0 */
    41    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh39 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh39[1] =
{
    /* RxPdu 0 */
    42    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh40 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh40[1] =
{
    /* RxPdu 0 */
    44    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh41 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh41[1] =
{
    /* RxPdu 0 */
    46    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh42 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh42[1] =
{
    /* RxPdu 0 */
    47    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh43 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh43[1] =
{
    /* RxPdu 0 */
    45    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh44 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh44[1] =
{
    /* RxPdu 0 */
    48    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh45 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh45[1] =
{
    /* RxPdu 0 */
    52    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh46 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh46[1] =
{
    /* RxPdu 0 */
    49    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh47 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh47[1] =
{
    /* RxPdu 0 */
    53    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh48 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh48[1] =
{
    /* RxPdu 0 */
    40    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh49 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh49[1] =
{
    /* RxPdu 0 */
    51    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh50 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh50[1] =
{
    /* RxPdu 0 */
    54    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh51 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh51[1] =
{
    /* RxPdu 0 */
    50    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh52 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh52[1] =
{
    /* RxPdu 0 */
    55    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh53 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh53[1] =
{
    /* RxPdu 0 */
    56    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh54 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh54[1] =
{
    /* RxPdu 0 */
    35    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh55 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh55[2] =
{
    /* RxPdu 0 */
    26,     
    /* RxPdu 1 */
    27    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh56 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh56[1] =
{
    /* RxPdu 0 */
    57    
};

/**
  * \brief This buffer contains list of RxPdus for Hrh57 of HoH0 Driver0
  *
  */
static CONST(CanIfLpduId_t, CANIF_CONST_PB) 
CanIf_RxPduList0Drv0Hrh57[1] =
{
    /* RxPdu 0 */
    58    
};







/**
  * \brief This container contains the configurable
  *  parameters of each CAN HRHs of Driver 0
  *
  */
static CONST(CanIfHrhConfigInfo_t, CANIF_CONST_PB) 
CanIf_HrhConfigInfo0Drv0_a[58] =
{
    /* HRH 0 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh0,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        0,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 1 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh1,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        1,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 2 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh2,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        2,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 3 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh3,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        3,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 4 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh4,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        4,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 5 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh5,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        5,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 6 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh6,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        6,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 7 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh7,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        7,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 8 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh8,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        8,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 9 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh9,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        9,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 10 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh10,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        10,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 11 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh11,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        11,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 12 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh12,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        12,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 13 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh13,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        13,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 14 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh14,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        14,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 15 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh15,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        15,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 16 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh16,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        16,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 17 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh17,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        17,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 18 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh18,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        18,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 19 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh19,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        19,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 20 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh20,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        20,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 21 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh21,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        21,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 22 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh22,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        22,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 23 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh23,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        23,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 24 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh24,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        24,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 25 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh25,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        25,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 26 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh26,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        26,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 27 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh27,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        27,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 28 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh28,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        28,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 29 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh29,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        29,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 30 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh30,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        30,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 31 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh31,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        31,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 32 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh32,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        32,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 33 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh33,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        33,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 34 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh34,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        34,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 35 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh35,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        35,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 36 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh36,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        36,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 37 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh37,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        37,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 38 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh38,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        38,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 39 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh39,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        39,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 40 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh40,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        40,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 41 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh41,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        41,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 42 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh42,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        42,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 43 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh43,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        43,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 44 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh44,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        44,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 45 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh45,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        45,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 46 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh46,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        46,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 47 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh47,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        47,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 48 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh48,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        48,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 49 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh49,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        49,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 50 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh50,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        50,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 51 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh51,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        51,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 52 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh52,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        52,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 53 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh53,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        53,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 54 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh54,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        54,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 55 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_BASIC_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,         
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh55,        
        /**< No: of RxPDUIds configured for the HRH */
        2,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        55,
        /**< Software filtering is enabled or not */
        STD_ON,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 56 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh56,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        56,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }, 
    /* HRH 57 */
    {
        /**< Defines the HRH type */
        CANIF_HOH_FULL_CAN,
        /**< Defines the parameters required for configuring multiple
             CANID ranges for a  given same HRH */
         NULL_PTR,        
        /**< List of RxPDUIds configured for the HRH */      
         CanIf_RxPduList0Drv0Hrh57,        
        /**< No: of RxPDUIds configured for the HRH */
        1,
        /**< The parameter refers to a particular HRH object in the
             CAN Driver Module */
        57,
        /**< Software filtering is enabled or not */
        STD_OFF,
        /**< The parameter refers to number of Hrh ranges configured */
        0
    }
};


/**
  * \brief This container contains the configurable
  *  parameters of each CAN HRHs
  *
  */
static CONSTP2CONST(CanIfHrhConfigInfo_t, CANIF_CONST_PB, CANIF_APPL_CONST)
CanIf_HrhConfigInfo0_a[1] =
{
    /* Driver 0 */
    CanIf_HrhConfigInfo0Drv0_a
    
};



/**
  * \brief This container contains the information related to transmit and
  *  receive buffers. It contains Postbuild time configurable parameters
  *
  */
static CONST(CanIfBufferInfo_t, CANIF_CONST_PB) 
CanIf_BufferInfo0 = 
{
    /**< Defines the size of transmit buffer */
    0,
    /**< Defines the size of Rx L-PDU data buffer */
    0,
    /**< Defines the size of Rx L-PDU interrupt decoupling buffer */
    513,
    /**< Size of the buffer holding indication status of RxPdus */
    8
};


#define CANIF_STOP_SEC_POSTBUILD_CONST_UNSPECIFIED  /* PRQA S 3614 */ 
#include "CanIf_MemMap.h"  /* PRQA S 5087 */ /* KW MISRA.INCL.INSIDE */


#define CANIF_START_SEC_POSTBUILD_VAR_NO_INIT_8  /* PRQA S 3614 */ 
#include "CanIf_MemMap.h"  /* PRQA S 5087 */ /* KW MISRA.INCL.INSIDE */

/* if RxInterruptDecoupling is enabled */
#if (CANIF_RX_INT_DECOUPLING == STD_ON)

/**
  * \brief This buffer holds the data of each received LPDUs for which
  *  interrupt decoupling is enabled. Size of buffer is
  *  total of (DLC + 1[for storing Dlc]) of RxPdus for which
  *  interrupt decoupling is enabled
  *
  */
static VAR(uint8, CANIF_VAR_PB) CanIf_RxPduIntDecDataBuf0_au8[513];

/**
  * \brief This variable holds the bit position of the indication status of
  *  RxPDUs for which Interrupt decoupling is enabled. Size of buffer is
  *  (total no: of RxPdus / 8)
  *
  */
static VAR(uint8, CANIF_VAR_PB) CanIf_RxPduIntDecNotifySt0_au8[8];

#endif /* End #if (CANIF_RX_INT_DECOUPLING == STD_ON) */

#define CANIF_STOP_SEC_POSTBUILD_VAR_NO_INIT_8  /* PRQA S 3614 */ 
#include "CanIf_MemMap.h"  /* PRQA S 5087 */ /* KW MISRA.INCL.INSIDE */

#define CANIF_START_SEC_POSTBUILD_VAR_NO_INIT_8  /* PRQA S 3614 */ 
#include "CanIf_MemMap.h"  /* PRQA S 5087 */ /* KW MISRA.INCL.INSIDE */



/* if ReadRxPduNotifyStatus API is enabled  */
#if (CANIF_READRXPDU_NOTIF_STATUS_API == STD_ON)
/**
  * \brief This variable holds the bit position of the indication status of
  *  RxPDUs for which ReadRxPduData is enabled. Size of buffer is
  *  (total no: of RxPdus / 8)
  *
  */
static VAR(uint8, CANIF_VAR_PB) CanIf_ReadRxPduNotifySt0_au8[8];

#endif /* End #if (CANIF_READRXPDU_NOTIF_STATUS_API == STD_ON) */


/* if ReadTxPduNotifyStatus API is enabled  */
#if (CANIF_READTXPDU_NOTIF_STATUS_API == STD_ON)
/**
  * \brief This variable holds the bit position of the indication status of
  *  TxPDUs for which ReadTxNotifyStatus is enabled. Size of buffer is
  *  (total no: of TxPdus / 8)
  *
  */
static VAR(uint8, CANIF_VAR_PB) CanIf_ReadTxPduNotifySt0_au8[2];

#endif /* End #if (CANIF_READTXPDU_NOTIF_STATUS_API == STD_ON) */

#define CANIF_STOP_SEC_POSTBUILD_VAR_NO_INIT_8  /* PRQA S 3614 */ 
#include "CanIf_MemMap.h"  /* PRQA S 5087 */ /* KW MISRA.INCL.INSIDE */


#define CANIF_START_SEC_POSTBUILD_CONST_UNSPECIFIED  /* PRQA S 3614 */ 
#include "CanIf_MemMap.h"  /* PRQA S 5087 */ /* KW MISRA.INCL.INSIDE */

static CONST(uint32, CANIF_CONST_PB) CanIf_PBtoPCHashValue = (uint32)912334548U;
static CONST(uint32, CANIF_CONST_PB) CanIf_PBtoLTHashValue = (uint32)3465002331U;

/**
  * \brief This data structure shall contain all the post build
  *  initialization data for the CAN Interface
  *
  */
  
CONST(CanIf_ConfigType, CANIF_CONST_PB) CanIfInitCfg =
{
        /**< Points to InitConfiguration values of CanIf module */
        &CanIf_InitConfiguration0,

        /**< Points to the post build configurable values of TxPdu  */
               CanIf_TxPduConfigInfo0_a, 
        /**< Points to the post build configurable values of RxPdu  */
               CanIf_RxPduConfigInfo0_a, 
        /**< Points to the post build configurable values of HRH  */
                CanIf_HrhConfigInfo0_a, 
        /**< Points to Rx and Tx buffer information of CanIf module */
        &CanIf_BufferInfo0,

/* if RxInterruptDecoupling is enabled */
#if (CANIF_RX_INT_DECOUPLING == STD_ON)

        /**< Points to inetrrupt decoupling data RAM buffer  */
                CanIf_RxPduIntDecDataBuf0_au8,         
        /**< Points to RxPdu inetrrupt decoupling indication status RAM buffer  */
        CanIf_RxPduIntDecNotifySt0_au8,

#endif /* End #if (CANIF_RX_INT_DECOUPLING == STD_ON) */

/* if ReadRxPduData API is enabled */
#if (CANIF_READRXPDU_DATA_API == STD_ON)

        /**< Points to RxPdu data RAM buffer  */
        NULL_PTR,
#endif /* End #if (CANIF_READRXPDU_DATA_API == STD_ON) */

/* if ReadRxPduNotifyStatus API is enabled  */
#if (CANIF_READRXPDU_NOTIF_STATUS_API == STD_ON)

        /**< Points to RxPdu notify status RAM buffer  */
        CanIf_ReadRxPduNotifySt0_au8,

#endif /* End #if (CANIF_READRXPDU_NOTIF_STATUS_API == STD_ON) */

        /**< Points to transmission data RAM buffer  */
                 NULL_PTR,        
/* if ReadTxPduNotifyStatus API is enabled  */
#if (CANIF_READTXPDU_NOTIF_STATUS_API == STD_ON)

        /**< Points to TxPdu notify status RAM buffer  */
        CanIf_ReadTxPduNotifySt0_au8,

#endif /* #if (CANIF_READTXPDU_NOTIF_STATUS_API == STD_ON) */

/* if ReadTxPduNotifyStatus API is enabled  */
#if (CANIF_SETDYNAMICTXID_API == STD_ON)

        /**< Points to TxPdu notify status RAM buffer  */
        NULL_PTR,
#endif/* #if (CANIF_SETDYNAMICTXID_API == STD_ON) */

#if (CANIF_PN_SUPPORT == STD_ON)

        /**< Total number of Pn Filter Pdus */
        CanIf_NoOfPnFilterPduIds,

#endif /* #if (CANIF_PN_SUPPORT == STD_ON) */

        /**< Hash key between Post Build and Pre compile  */
        &CanIf_PBtoPCHashValue,

        /**< Hash key between Post Build and Link Time  */
        &CanIf_PBtoLTHashValue
};

#define CANIF_STOP_SEC_POSTBUILD_CONST_UNSPECIFIED  /* PRQA S 3614 */ 
#include "CanIf_MemMap.h"  /* PRQA S 5087 */ /* KW MISRA.INCL.INSIDE */


/*============================== END OF FILE CanIf_PbCfg.c ===================*/
